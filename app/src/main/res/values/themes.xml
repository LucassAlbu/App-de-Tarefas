<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AppdeTarefas" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/primaria01</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowBackground">@color/primaria01</item>
    </style>

    <style name="Theme.AppdeTarefas" parent="Base.Theme.AppdeTarefas" />

    <style name="custom_tabLayout" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="custom_radio_button" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:buttonTint">@color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>